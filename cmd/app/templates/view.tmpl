{{ define "view.go" }}package ui

import (
	"net/http"

	"{{ .Module }}/internal/app/config"
)

// View defines an HTTP handler function for rendering a template with data.
func View(cfg *config.Config, name string, data any) http.HandlerFunc {
	return cfg.Templating.View(name, data)
}

{{ end }}
